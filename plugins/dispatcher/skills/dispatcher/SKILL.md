---
name: dispatcher
description: 当用户明确要求多 subagent 实现、main 只作调度和 review，或主动调用 dispatcher 时使用。也应在用户要求开始执行整份已确认的 spec、规格或多步骤实施计划时使用；普通实施请求则在需求明确、横跨多个模块且存在值得独立推进的实现切片时使用。由 subagent 完成实现和修复，main 负责拆解、调度、审查和统一验证，默认中文交互。仅讨论、设计或审查 spec、读取解释代码不触发。未明确要求委派的单点文案修改和局部小修复，即使引用已有 spec，也不因提到规格而触发；仓库存在规格文件不代表授权开始实现。
---

# Dispatcher

将当前实施任务交给 subagent 完成，Main 负责调度、审查和验收。本流程覆盖当前任务及其修复轮次，不扩展到之后无关的请求；遵守更高优先级指令和仓库规则。

## 确认执行目标

从当前请求、附带任务和已有对话中确定目标，读取相关规格，复用已确认的范围和决策，不重新开启已完成的设计访谈。设计内容不等于实施授权；遇到尚未确认的产品决策时，先完成必要的设计确认，再启动受影响的实现。

主动调用时，从附带任务或已有对话中确定目标；两者都没有可执行目标时，用中文询问要实现的任务或规格，不自行选择项目工作。

## 语言与职责

默认使用简体中文进行问答、进度说明、任务分派、审查反馈和最终交付。给每位 subagent 明确传达此要求，其汇报也使用中文；用户明确指定其他语言时遵从用户。代码标识符、路径、命令、API 名称和原始报错保留原文，代码注释和项目文档遵循仓库约定。

Main 负责范围、验收标准、拆解、共享契约、调度、review 和最终验证。派工前读取必要上下文，理解已有实现和项目约定。

所有交付文件的修改都交给 subagent，包括实现、测试、文档、集成和修复。保持这一边界，让 Main 留出注意力检查整体契约，也避免审查者临时接管修改后漏掉复核。Main 不亲自修改，即使只是小修补，也不通过 shell/eval 代写。Main 可以读取文件、检查 diff、跟踪任务、协调代理和运行不改写交付文件的检查；格式化等会改写交付文件的操作也交给 subagent。

本流程的调度者是接收用户实施请求的主会话。给实现 subagent 的委派应明确：直接完成获派切片，不递归套用完整调度流程；确需继续委派时先由 Main 决定。这样既保留整体协调，也确保实现工作有直接负责人。

## 拆解与派工

1. 从需求或规格提取可观察的验收条件，确定依赖关系、文件/符号归属和跨切片接口。接口、数据格式等共享契约由 Main 在派工前确定；无法从仓库和已确认设计推导出的重大决策才询问用户。
2. 选择最匹配的可用代理类型，只读探索使用 scout。对真正独立的实现切片，在同一个 task 批次中并行启动；有依赖的切片分波次执行。根据实际归属边界和宿主限制决定并发度，不固定人数。实际工作不可拆分时，使用一名实现代理，Main 保持审查职责；人为拆分只会增加协调和冲突成本。
3. 为共享文件指定唯一的集成负责人，并串行安排不可避免的共享修改。代理跨越归属边界前必须协调，不允许并发覆盖彼此或用户的改动。
4. 每份委派必须自包含，至少给出：目标、相关上下文、精确文件/符号范围、非目标、接口契约、依赖、可观察的验收条件、中文汇报要求。不要假定 subagent 继承完整对话，也不要把顶层拆解工作整体转交给代理。
5. 明确要求代理在并行编辑期间跳过构建、格式化、lint 和测试运行，由 Main 在编辑收敛后统一安排验证。若仓库要求先验证失败用例，将该验证作为实现前的独立阶段，再启动对应实现批次，不能用并行安排跳过测试纪律。
6. 保留用户已有改动；未经用户要求，不授权提交、推送、破坏性操作或无关修改。

### 派工示例

用户要求按已确认规格实现审计日志导出。Main 读取现有代码后，先确定导出请求参数、权限要求和错误响应格式，再识别文件归属：

- **同批启动**：代理 A 修改服务层的导出查询与序列化；代理 B 修改客户端导出面板。两者使用已确定的 API 契约，分别负责各自模块及相关测试代码，不修改共享路由入口，并行编辑期间不运行检查。
- **后续集成**：A、B 完成后，由 A 按追加的集成委派修改共享路由入口、连接服务并同步接口文档；B 如需契约调整，先交给 Main 决策，不自行更改服务端约定。
- **审查与修复**：Main 检查真实改动，发现导出权限遗漏则派回 A 修复，复核后统一运行相关验证，再对照规格验收。

这里的并发来自提前确定的契约和互不重叠的修改范围，而不是“服务端、客户端”这些标签。若客户端必须等服务端探索结果才能确定接口，就先完成契约探索，再启动实现批次。

## 调度与 Review

- 及时处理依赖决策，其他独立切片执行时审查已完成部分。避免反复轮询或手把手遥控代理；只有没有可推进的调度和审查工作时才等待。
- 要求代理报告修改文件、实现行为、假设和未解决风险。代理声称完成只是审查输入，不是验收证据。
- 检查真实改动是否符合需求、既有约定、正确性、边界条件、安全性和跨切片兼容性。对照验收条件检查遗漏，不能只看代码是否编译。
- 将具体问题及其位置、风险、预期行为派回对应代理。集成和修复仍由代理完成，之后再次 review；不要因为改动小就接管实现。

## 验收与交付

编辑收敛后，Main 按用户要求和仓库约定运行相关检查，并实际执行变更路径；会改写交付文件的检查交给代理。失败派回代理修复，再做受影响的验证。没有观察到输出，不宣称测试通过；用户要求自行验证时，明确列出未验证范围。

已有规格覆盖的行为变化，由代理将对应规格同步到实现，并遵循仓库文档约定，不另建重复规格。若实施会改写已确认的产品承诺、范围、数据契约或验收标准，先向用户说明冲突并取得决策，再继续受影响的部分。

持续推进到全部验收条件满足，或确认存在无法取得的外部前提。若无法使用 subagent，明确报告限制，不自行切换成 Main 实现。最终用中文说明交付行为、验证证据、未验证范围和真实阻塞项；区分已实现与已验证，不重复代理的全部过程汇报。
